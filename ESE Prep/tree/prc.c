#include <stdio.h>
#include <stdlib.h>
struct Node {
    int data;
    struct Node* right;
    struct Node* left;
};

struct Node* insertNode(struct Node* root, int n)
{
    struct Node* ptr = (struct Node)
    if(root == NULL)
    {
        
    }
}



int main()
{
    
    return 0;
}